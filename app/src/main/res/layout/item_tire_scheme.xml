<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:focusableInTouchMode="true"
    android:paddingStart="10dp"
    android:paddingLeft="10dp"
    android:paddingEnd="10dp"
    android:paddingRight="10dp">

    <TextView
        android:id="@+id/tire_scheme_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="tire scheme"
        android:textColor="@android:color/black"/>

    <android.support.design.widget.TextInputLayout
        android:layout_width="match_parent"
        android:theme="@style/TextLabel"
        android:id="@+id/tire_scheme_input_layout"
        android:layout_height="wrap_content">

        <android.support.design.widget.TextInputEditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:imeOptions="actionDone"
            android:textColor="@android:color/black"
            android:drawableEnd="@drawable/ic_pen"
            android:singleLine="true"
            android:drawableRight="@drawable/ic_pen"
            android:id="@+id/tire_scheme_edit_text"
            android:hint="@string/add_value_hint"/>

    </android.support.design.widget.TextInputLayout>

</LinearLayout>

